# --- Web & API ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.12
python-dotenv==1.0.1
pydantic-settings==2.6.1

# --- Database ---
sqlalchemy==2.0.35
aiosqlite==0.20.0  # SQLite async driver
psycopg[binary]  # PostgreSQL driver (optional, for PostgreSQL)
alembic==1.13.3

# --- Computer Vision & AI ---
# YOLOv11 for object detection
ultralytics==8.3.0
opencv-python-headless==4.10.0.84  # Headless is lighter for servers/FastAPI

# PyTorch and Transformers for Moondream2
torch>=2.0.0  # PyTorch for Moondream2 (supports MPS on M4 Macs)
transformers>=4.40.0  # Hugging Face transformers for Moondream2
Pillow>=10.0.0  # PIL for image processing

# CLIP for semantic embeddings
open-clip-torch==2.24.0  # OpenCLIP for image embeddings

# Dimensionality reduction for 2D visualization
umap-learn==0.5.5  # UMAP for reducing 512D to 2D
scikit-learn==1.4.0  # For additional ML utilities

# --- Core Dependencies ---
numpy==1.26.4
